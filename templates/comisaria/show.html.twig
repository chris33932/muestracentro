{% extends 'base.html.twig' %}

{% block title %}Comisaria{% endblock %}

{% block body %}
    <h1>Comisaria</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comisarium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ comisarium.nombre }}</td>
            </tr>
            <tr>
                <th>Calle</th>
                <td>{{ comisarium.calle }}</td>
            </tr>
            <tr>
                <th>Altura</th>
                <td>{{ comisarium.altura }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('comisaria_index') }}">back to list</a>

    <a href="{{ path('comisaria_edit', {'id': comisarium.id}) }}">edit</a>

    {{ include('comisaria/_delete_form.html.twig') }}
{% endblock %}
