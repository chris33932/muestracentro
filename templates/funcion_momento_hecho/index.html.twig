{% extends 'base.html.twig' %}

{% block title %}FuncionMomentoHecho index{% endblock %}

{% block body %}
    <h1>FuncionMomentoHecho index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for funcion_momento_hecho in funcion_momento_hechos %}
            <tr>
                <td>{{ funcion_momento_hecho.id }}</td>
                <td>{{ funcion_momento_hecho.descripcion }}</td>
                <td>
                    <a href="{{ path('funcion_momento_hecho_show', {'id': funcion_momento_hecho.id}) }}">show</a>
                    <a href="{{ path('funcion_momento_hecho_edit', {'id': funcion_momento_hecho.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('funcion_momento_hecho_new') }}">Create new</a>
{% endblock %}
