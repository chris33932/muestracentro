<form method="post" action="{{ path('funcion_momento_hecho_delete', {'id': funcion_momento_hecho.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ funcion_momento_hecho.id) }}">
    <button class="btn">Delete</button>
</form>
