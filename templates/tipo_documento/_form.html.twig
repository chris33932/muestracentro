<div class="container" style="margin-top:30px">
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn" class="btn btn-info">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
</div>
