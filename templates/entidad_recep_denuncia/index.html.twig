{% extends 'base.html.twig' %}

{% block title %}EntidadRecepDenuncia index{% endblock %}

{% block body %}
    <h1>EntidadRecepDenuncia index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entidad_recep_denuncium in entidad_recep_denuncias %}
            <tr>
                <td>{{ entidad_recep_denuncium.id }}</td>
                <td>{{ entidad_recep_denuncium.descripcion }}</td>
                <td>
                    <a href="{{ path('entidad_recep_denuncia_show', {'id': entidad_recep_denuncium.id}) }}">show</a>
                    <a href="{{ path('entidad_recep_denuncia_edit', {'id': entidad_recep_denuncium.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('entidad_recep_denuncia_new') }}">Create new</a>
{% endblock %}
