{% extends 'base.html.twig' %}

{% block title %}FranjaHoraria index{% endblock %}

{% block body %}
    <h1>FranjaHoraria index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for franja_horarium in franja_horarias %}
            <tr>
                <td>{{ franja_horarium.id }}</td>
                <td>{{ franja_horarium.descripcion }}</td>
                <td>
                    <a href="{{ path('franja_horaria_show', {'id': franja_horarium.id}) }}">show</a>
                    <a href="{{ path('franja_horaria_edit', {'id': franja_horarium.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('franja_horaria_new') }}">Create new</a>
{% endblock %}
