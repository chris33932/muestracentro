{% extends 'base.html.twig' %}

{% block title %}Genero{% endblock %}

{% block body %}{{ parent() }}
    <h1>Genero</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ genero.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ genero.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('genero_index') }}">back to list</a>

    <a href="{{ path('genero_edit', {'id': genero.id}) }}">edit</a>

    {{ include('genero/_delete_form.html.twig') }}
{% endblock %}
