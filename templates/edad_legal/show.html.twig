{% extends 'base.html.twig' %}

{% block title %}EdadLegal{% endblock %}

{% block body %}
    <h1>EdadLegal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ edad_legal.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ edad_legal.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('edad_legal_index') }}">back to list</a>

    <a href="{{ path('edad_legal_edit', {'id': edad_legal.id}) }}">edit</a>

    {{ include('edad_legal/_delete_form.html.twig') }}
{% endblock %}
