{% extends 'base.html.twig' %}

{% block title %}EstadoIntox{% endblock %}

{% block body %}
    <h1>EstadoIntox</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ estado_intox.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ estado_intox.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('estado_intox_index') }}">back to list</a>

    <a href="{{ path('estado_intox_edit', {'id': estado_intox.id}) }}">edit</a>

    {{ include('estado_intox/_delete_form.html.twig') }}
{% endblock %}
