<form method="post" action="{{ path('estado_intox_delete', {'id': estado_intox.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ estado_intox.id) }}">
    <button class="btn">Delete</button>
</form>
