{% extends 'base.html.twig' %}

{% block title %}Edit Ocupacion{% endblock %}

{% block body %}{{ parent() }}
    <h1>Editar</h1>

    {{ include('ocupacion/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('ocupacion_index') }}">back to list</a>

    {{ include('ocupacion/_delete_form.html.twig') }}
{% endblock %}
