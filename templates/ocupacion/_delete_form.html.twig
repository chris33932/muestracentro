<form method="post" action="{{ path('ocupacion_delete', {'id': ocupacion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ocupacion.id) }}">
    <button class="btn">Delete</button>
</form>
