<form method="post" action="{{ path('dia_semana_delete', {'id': dia_semana.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dia_semana.id) }}">
    <button class="btn">Delete</button>
</form>
