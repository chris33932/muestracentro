{% extends 'base.html.twig' %}

{% block title %}DiaSemana index{% endblock %}

{% block body %}
    <h1>DiaSemana index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for dia_semana in dia_semanas %}
            <tr>
                <td>{{ dia_semana.id }}</td>
                <td>{{ dia_semana.descripcion }}</td>
                <td>
                    <a href="{{ path('dia_semana_show', {'id': dia_semana.id}) }}">show</a>
                    <a href="{{ path('dia_semana_edit', {'id': dia_semana.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('dia_semana_new') }}">Create new</a>
{% endblock %}
