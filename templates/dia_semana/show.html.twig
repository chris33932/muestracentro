{% extends 'base.html.twig' %}

{% block title %}DiaSemana{% endblock %}

{% block body %}
    <h1>DiaSemana</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dia_semana.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ dia_semana.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dia_semana_index') }}">back to list</a>

    <a href="{{ path('dia_semana_edit', {'id': dia_semana.id}) }}">edit</a>

    {{ include('dia_semana/_delete_form.html.twig') }}
{% endblock %}
