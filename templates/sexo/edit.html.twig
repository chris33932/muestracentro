{% extends 'base.html.twig' %}

{% block title %}Editar{% endblock %}

{% block body %}{{ parent() }}
    <h2>Editar categoria</h2>

    {{ include('sexo/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('sexo_index') }}">Volver</a>

    {{ include('sexo/_delete_form.html.twig') }}
{% endblock %}
