<form method="post" action="{{ path('estado_policial_delete', {'id': estado_policial.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ estado_policial.id) }}">
    <button class="btn">Delete</button>
</form>
