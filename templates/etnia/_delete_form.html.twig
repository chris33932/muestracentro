<form method="post" action="{{ path('etnia_delete', {'id': etnium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etnium.id) }}">
    <button class="btn">Delete</button>
</form>
