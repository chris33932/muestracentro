{% extends 'base.html.twig' %}

{% block title %}Etnia{% endblock %}

{% block body %}
    <h1>Etnia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etnium.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ etnium.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('etnia_index') }}">back to list</a>

    <a href="{{ path('etnia_edit', {'id': etnium.id}) }}">edit</a>

    {{ include('etnia/_delete_form.html.twig') }}
{% endblock %}
