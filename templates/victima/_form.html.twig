
<div class="container" style="margin-top:30px">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-success">{{ button_label|default('Guardar') }}</button>
            
            <button class="btn btn-info"> <a href="{{ path('victima_index') }}">Volver a la lista</a></button>
               <br>
        {{ form_end(form) }}
   <br>
 
</div>
  <br>